package demo.calendar.repository

import jakarta.persistence.*

@Entity
@Table(name = "calendars")
class CalendarRepository (
    //мы сделаем этот репозиторий позже т. к. не знаем пока как интегрироваться с внешними серверами
)